{
  "build": {
    "lfs": true,
    "nixpacks": {
      "config": {
        "setup": [
          "apt-get update",
          "apt-get install -y libgl1 git-lfs",
          "git lfs install"
        ]
      }
    }
  }
}
